@page "/register"
@rendermode InteractiveServer

<div class="container-form">
    <div class="form">
        <EditForm Model="@RegisterViewModel" OnValidSubmit="HandleValidSubmit">
            <h1>Register</h1>
            <DataAnnotationsValidator />
            <ValidationSummary class="error-text" />

            <div class="form-content">
                <InputText @bind-Value="RegisterViewModel.FirstName" class="form-input" placeholder="First Name" />
                <ValidationMessage For="() => RegisterViewModel.FirstName" class="error-text" />
            </div>

            <div class="form-content">
                <InputText @bind-Value="RegisterViewModel.LastName" class="form-input" placeholder="Last Name" />
                <ValidationMessage For="() => RegisterViewModel.LastName" class="error-text" />
            </div>

            <div class="form-content">
                <InputText @bind-Value="RegisterViewModel.Email" class="form-input" type="email" placeholder="Email" />
                <ValidationMessage For="() => RegisterViewModel.Email" class="error-text" />
            </div>

            <div class="form-content">
                <InputText @bind-Value="RegisterViewModel.UserName" class="form-input" placeholder="Login" />
                <ValidationMessage For="() => RegisterViewModel.UserName" class="error-text" />
            </div>

            <div class="form-password">
                <InputText @bind-Value="RegisterViewModel.Password" class="form-input password" type="password" placeholder="Password" />
                <ValidationMessage For="() => RegisterViewModel.Password" class="error-text" />
            </div>

            <button type="submit" class="form-btn">Register</button>

            <p class="form-loggined">Already registered? <a href="/login">Sign-in!</a></p>

            <span class="label label-error">@Message</span>
        </EditForm>
    </div>
</div>